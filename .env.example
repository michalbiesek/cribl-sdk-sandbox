# Cribl Cloud Configuration (OAuth2)
# For Cloud examples in examples/control-plane/{python,typescript,go}/cloud_list_worker_group.*

# Your Cribl Cloud organization ID
CRIBL_ORG_ID=your-org-id

# OAuth2 client ID for API authentication
CRIBL_CLIENT_ID=your-client-id

# OAuth2 client secret for API authentication
CRIBL_CLIENT_SECRET=your-client-secret

# Workspace name (defaults to "main" if not set)
CRIBL_WORKSPACE_NAME=main

# Domain for Cribl Cloud (defaults to cribl.cloud if not set)
CRIBL_DOMAIN=cribl.cloud

# -----------------------------------------------------------------------------
# On-Premise Configuration (for on-prem examples)
# -----------------------------------------------------------------------------
# Examples:
#   HTTP:  http://localhost:19000
#   HTTPS: https://localhost:9000 or https://cribl.example.com:8000

CRIBL_SERVER_URL=http://localhost:19000
CRIBL_USERNAME=admin
CRIBL_PASSWORD=admin

# -----------------------------------------------------------------------------
# TLS Configuration (HTTPS only)
# -----------------------------------------------------------------------------
# When connecting to HTTPS endpoints with self-signed certificates:
#   true  = Accept self-signed certificates (development/testing)
#   false = Enforce strict certificate verification (production)
# 
# This setting only applies when CRIBL_SERVER_URL starts with "https://"
CRIBL_INSECURE_TLS=true